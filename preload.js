//A preload script runs before the renderer process is loaded, 
//and has access to both renderer globals (e.g. window and document) and a Node.js environment.

console.log("EXECUTED BEFORE THE WINDOW HAS LOADED")